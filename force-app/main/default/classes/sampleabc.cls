public class sampleabc {
    public sampleabc(){
for(integer i=0;i>=0;i--){
        if(true)
        This(1);
                system.debug('ssss');
                }
    }
    public sampleabc(integer s){
        this();
        system.debug(s);
        
    }
}